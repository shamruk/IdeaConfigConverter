type = 'swf'

${frameworkLinkage.disabler}frameworkLinkage = 'merged'

//configurations {
//	all*.exclude group: 'com.adobe.flex.framework'
//}

dependencies {
	${dependencies.gradle}
}

srcDirs = [
		'${source.directory.main}'
]

mainClass = '${source.file.directory}/${source.file.name}.as'

additionalCompilerOptions = [
]

${frameworkLinkage.air.enabler}afterEvaluate {
${frameworkLinkage.air.enabler}	dependencies {
${frameworkLinkage.air.enabler}		external files("${flexHome}/frameworks/libs/air/airglobal.swc")
${frameworkLinkage.air.enabler}	}
${frameworkLinkage.air.enabler}}

//<groupId>${groupId}</groupId>
//	<artifactId>${artifactId}</artifactId>
//<packaging>swf</packaging>
//	<version>${version}</version>
//
//		<outputDirectory>${out.output.directory}</outputDirectory>
//<directory>${out.directory}</directory>
//							<file>${source.file.name}.as</file>
//<startDir>${project.basedir}/${source.directory.main}/${source.file.directory}</startDir>
//							<file>${source.file.name}.mxml</file>
//<startDir>${project.basedir}/${source.directory.main}/${source.file.directory}</startDir>
//
//
//
//					<finalName>${out.file}</finalName>
//
//
//<debug>${debug}</debug>
//					<optimize>${optimize}</optimize>
//<sourceFile>${source.file}</sourceFile>
//
//
//						<version>${flex.framework.version}</version>
//
//<version>${flex.framework.version}</version>
//
//			<version>${flex.framework.version}</version>
//<classifier>${flash.player.version}</classifier>
//			<url>${repository.local.generated.url}</url>